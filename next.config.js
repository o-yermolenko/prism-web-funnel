/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: [],
  },
  env: {
    SITE_NAME: 'The Prism',
  },
}

module.exports = nextConfig

